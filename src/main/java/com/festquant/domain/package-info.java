/**
 * Core business entities used by the festival revenue-management system.
 *
 * <p>These classes model real project concepts such as events, users, bids,
 * forecasts and price recommendations. Their fields are private and exposed
 * through controlled constructors/getters, which demonstrates encapsulation and
 * keeps invalid business state from leaking across the application.</p>
 */
package com.festquant.domain;
